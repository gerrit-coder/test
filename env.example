# Environment Configuration for Coder-Gerrit Integration Test
# Copy this file to .env and customize the values for your environment

# Gerrit Configuration
GERRIT_URL="http://127.0.0.1:8080"

# Coder Configuration
CODER_PORT="3000"
CODER_ACCESS_URL="http://127.0.0.1:3000"
CODER_URL="http://127.0.0.1:3000"

# Nginx Proxy Configuration (for CORS handling)
# Use CODER_PROXY_URL in your Gerrit plugin configuration to avoid CORS issues
NGINX_PORT="3001"
CODER_PROXY_URL="http://127.0.0.1:3001"

# Template Configuration
CODER_TEMPLATE_NAME="vscode-web"

# Docker Configuration (required for docker-compose.yml)
# Get the Docker group ID by running: getent group docker | cut -d: -f3
DOCKER_GROUP=""

# Coder Data Directory (for persistence)
# Default: ./coder-data (relative to docker-compose.yml location)
# Alternative: $HOME/.config/coderv2-docker (as per official docs)
# Note: This directory will be mounted to /home/coder/.config/coderv2 in the container
CODER_DATA="./coder-data"

# Authentication (set these when you have them)
# CODER_SESSION_TOKEN="your-coder-session-token"

# Example for production environments:
# GERRIT_URL="https://gerrit.yourcompany.com"
# CODER_ACCESS_URL="https://coder.yourcompany.com"
# CODER_URL="https://coder.yourcompany.com"
# CODER_PROXY_URL="https://nginx-proxy.yourcompany.com"
